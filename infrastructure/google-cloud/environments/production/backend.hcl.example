# Placeholder only; this source does not define or approve a state bucket.
# Before replacement, separately verify production isolation, UBLA,
# public-access prevention, versioning/retention, encryption decision, keyless
# least-privilege IAM, recovery access, and audit logs. Never reuse staging.
bucket = "OWNER_APPROVED_PRODUCTION_TERRAFORM_STATE_BUCKET"
prefix = "fci-operations/production"
